# OpenSpec：`zType` 复刻项目规划（中文）

## 1. 项目概述

本项目目标是在浏览器中复刻一个 `zType` 风格的打字射击游戏核心体验：

- 敌人携带单词出现并向下推进。
- 玩家通过键盘输入字母/单词进行锁定与击杀。
- 难度随时间提升，具备计分、生命、关卡与失败重开。

实现策略采用 **渐进式迭代**：先保证核心可玩，再逐步补齐特效、音频、UI、平衡性与扩展能力。

## 2. 目标与非目标

### 2.1 目标（Goals）

1. 在纯前端（`HTML + CSS + JavaScript + Canvas`）下实现可运行版本。
2. 复现关键玩法机制：
   - 首字母匹配锁定目标。
   - 连续输入命中目标单词。
   - 击毁反馈、漏怪扣血、游戏结束。
3. 提供基础可配置参数（速度、生成频率、词库、难度曲线）。
4. 代码结构清晰，便于后续添加新模式或移动端支持。

### 2.2 非目标（Non-Goals）

1. 不追求像素级视觉还原原版素材。
2. 不在首版接入后端排行榜或账号系统。
3. 不在首版支持多人联机或复杂剧情系统。

## 3. 用户故事

1. 作为玩家，我希望输入字母后能自动选择合适目标，减少操作负担。
2. 作为玩家，我希望击杀反馈明显（子弹、爆炸、分数跳动），获得正向反馈。
3. 作为玩家，我希望难度逐步提升，但前期可快速上手。
4. 作为开发者，我希望词库与参数可单独配置，便于调优和扩展。

## 4. 功能需求

### 4.1 核心循环

- 游戏状态：`ready` / `running` / `gameover` / `paused(可选)`。
- 固定时间步或 `requestAnimationFrame` 更新循环。
- 每帧处理：输入队列 -> 逻辑更新 -> 碰撞判定 -> 渲染。

### 4.2 敌人与词库

- 敌人属性：`id`、`word`、`progress`、`x`、`y`、`speed`、`hp(可选)`。
- 生成规则：按时间间隔与最大并发控制生成。
- 词库分层：按长度或字母罕见度分等级。

### 4.3 输入与锁定

- 玩家输入字符时：
  1. 无目标：从存活敌人中匹配首字母并选最近/最危险目标。
  2. 有目标：仅接受与目标下一字符匹配的输入。
- 输入错误策略（首版）：忽略错误输入，不扣分。
- 目标被击毁后自动清空锁定。

### 4.4 战斗与反馈

- 每次有效字符输入触发一次射击反馈（视觉或粒子）。
- 单词输入完成后敌人销毁并加分。
- 敌人越界触底：扣生命并移除。

### 4.5 计分与难度

- 基础分：按单词长度计分。
- 连击（第二阶段）：连续命中递增倍率。
- 难度提升：随时间缩短生成间隔、增加下落速度、引入长词。

### 4.6 UI 与交互

- HUD：分数、生命、等级、当前目标词。
- 游戏结束层：最终分数 + 重开按钮。
- 可选：开始页、暂停提示、键位说明。

## 5. 非功能需求

1. **性能**：普通笔记本浏览器稳定 60 FPS（敌人 < 80 时）。
2. **可维护性**：模块边界明确，尽量避免全局变量污染。
3. **可读性**：命名语义化，避免单字母变量。
4. **兼容性**：支持现代 Chromium/Firefox/Safari。

## 6. 技术方案

### 6.1 技术栈

- `HTML5 Canvas`：游戏渲染
- `Vanilla JavaScript`：逻辑与输入
- `CSS`：HUD、覆盖层、基础视觉

### 6.2 建议目录结构

```text
ztypegamelab/
  index.html
  style.css
  game.js
  src/
    config.js
    words.js
    core/
      game-loop.js
      state.js
    entities/
      player.js
      enemy.js
      bullet.js
    systems/
      spawn-system.js
      input-system.js
      collision-system.js
      render-system.js
```

首版可先使用单文件 `game.js`，在第二阶段再模块化拆分。

### 6.3 关键数据结构（示意）

```js
enemy = {
  id: 1,
  word: "planet",
  progress: 0,
  x: 200,
  y: 40,
  speed: 35,
  isLocked: false
}
```

```js
gameState = {
  status: "running",
  score: 0,
  lives: 5,
  level: 1,
  enemies: [],
  bullets: [],
  lockedEnemyId: null,
  spawnIntervalMs: 1600
}
```

## 7. 迭代计划（分步复刻）

### 里程碑 M1：最小可玩版本（当前优先）

- Canvas 场景 + HUD
- 敌人下落与随机词生成
- 输入锁定 + 单词击杀
- 计分、生命、失败重开

### 里程碑 M2：手感增强

- 子弹飞行轨迹、爆炸粒子
- 连击与分数倍率
- 更平滑的难度曲线

### 里程碑 M3：体验完善

- 音效与背景音乐开关
- 菜单页与暂停
- 本地最高分（`localStorage`）

### 里程碑 M4：工程化（可选）

- 代码模块化拆分
- 单元测试（词匹配、目标选择）
- 构建工具接入（Vite 或原生 ES 模块）

## 8. 验收标准（DoD）

1. 打开 `index.html` 即可开始游戏（或明显的开始按钮）。
2. 输入字母能锁定并击杀匹配敌人，且不可同时锁定多个目标。
3. 分数、生命、等级实时更新且逻辑一致。
4. 敌人触底必定扣血，生命归零后进入结束态。
5. 点击“重新开始”可重置完整状态并再次游玩。

## 9. 风险与应对

1. **输入冲突风险**：浏览器快捷键/输入法影响按键事件。
   - 对策：仅监听 `a-z`，忽略组合键，提醒使用英文输入。
2. **性能抖动风险**：敌人增多导致帧率下降。
   - 对策：对象池、裁剪绘制、降低粒子数量。
3. **难度失衡风险**：前期过难或后期无聊。
   - 对策：参数外置并进行多轮试玩调优。

## 10. 运行与开发说明

首版建议直接使用浏览器打开 `index.html`。若需本地服务器，可用：

```bash
npx serve .
```

---

如果你确认这份 spec，我将按 `M1 -> M2 -> M3` 顺序逐步实现，每一步都给你可运行结果和改动说明。
